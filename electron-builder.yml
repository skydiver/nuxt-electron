productName: "nuxt-electron"
appId: "com.example.nuxt-electron"

electronVersion: "5.0.2"

files:
  - ".nuxt-build/**/*"
  - "electron"

directories:
  buildResources: "static"
  output: ".dist"

mac:
  target:
    - target: "dir"
